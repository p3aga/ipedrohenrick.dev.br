---
import { menuLinks, siteConfig } from "@/site.config"
---

<header>
  <div class="mt-20 mb-10">
    <div>
      <a href="/" class="text-xl font-serif font-black">{siteConfig.SiteName}</a>
    </div>
    <nav class="divide-x -ms-3 mt-5 divide-dashed">
      {
        menuLinks.map(link => (
          <a href={link.path} class="px-3 box-border font-medium">{link.title}</a>
        ))
      }
    </nav>
  </div>
</header>
